name: Integration Tests

on:
  workflow_dispatch:  # Manual trigger
  push:
    branches: [ main ]  # Only on main branch
  pull_request:
    branches: [ main ]

jobs:
  integration-tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11']
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install test dependencies
        run: |
          pip install pytest pytest-cov pytest-asyncio

      - name: Run integration tests
        env:
          QDRANT_URL: http://localhost:6333
          QDRANT_COLLECTION: test_collection
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          EMBEDDING_MODEL: text-embedding-3-small
          LLM_MODEL: gpt-4o-mini
        run: |
          # Run only mocked tests that don't require a real server
          pytest tests/integration/test_enhanced_frameworks_mocked.py tests/integration/test_working_frameworks_mocked.py -v --maxfail=1 --disable-warnings
          
          # Skip tests that require a real server (they make HTTP requests to localhost:8080)
          # pytest tests/integration/test_agentic_rag.py -v --maxfail=1 --disable-warnings
          # pytest tests/integration/test_personalized_coaching.py -v --maxfail=1 --disable-warnings
          # pytest tests/integration/test_monitoring.py -v --maxfail=1 --disable-warnings
